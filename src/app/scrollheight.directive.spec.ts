import { ScrollheightDirective } from './scrollheight.directive';

describe('ScrollheightDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollheightDirective();
    expect(directive).toBeTruthy();
  });
});
